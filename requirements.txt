# ── HTTP / config ──────────────────────────────────────────────
httpx==0.27.2
tenacity==8.3.0
pydantic==2.7.4
pydantic-settings==2.3.4
python-dotenv==1.0.1
requests==2.31.0

# ── LLM providers ─────────────────────────────────────────────
# (no extra package needed — all via httpx)

# ── Video rendering (replaces Creatomate) ─────────────────────
moviepy==1.0.3
Pillow>=9.5.0
numpy>=1.24.0
imageio==2.33.1
imageio-ffmpeg>=0.4.9
decorator==4.4.2          # moviepy 1.0.3 requires this exact pin

# ── Media / CDN (Cloudinary kept optional) ────────────────────
cloudinary==1.40.0

# ── Publishing ─────────────────────────────────────────────────
# Buffer API via httpx — no extra package needed

# Telegram Bot
python-telegram-bot==21.6
